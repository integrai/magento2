<?php
/**
 * @var Integrai\Core\Block\AllPages $block
 */

$scripts = $block->getIntegraiConfigs();

if(isset($scripts) && count($scripts) > 0) {
    foreach ($scripts as $script) {
        echo $script;
    }
}
?>